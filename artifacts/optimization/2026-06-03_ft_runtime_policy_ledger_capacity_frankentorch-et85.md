# ft-runtime Policy Ledger Capacity

Bead: `frankentorch-et85`
Agent: TurquoisePine
Outcome: kept.

## Target

Fallback profile-backed target after `br ready --json` had no unclaimed perf
work available at the time this bead was created.

Benchmark:

```text
runtime_policy_evidence/new_and_switch_1024
```

Fresh pre-change rch baseline recorded in the bead:

```text
worker: vmi1156319
time: [181.81 us 186.00 us 190.39 us]
```

Same-worker old-code evidence from the retained runtime policy summary pass:

```text
worker: vmi1153651
time: [223.21 us 242.46 us 264.21 us]
```

## One Lever

`RuntimeContext::new` now initializes its internal policy `EvidenceLedger` with
capacity for the two common policy entries produced by the benchmark:

1. initial mode record from `RuntimeContext::new`
2. switch record from `RuntimeContext::set_mode`

The public `EvidenceLedger::new` API still returns the previous empty ledger.
Only the private runtime-construction path uses the capacity hint.

## Isomorphism Proof

- Ordering: entries are still appended exclusively through
  `EvidenceLedger::record`, in the same call order.
- Tie-breaking: no comparisons or tie-breaking behavior exist in this path.
- Floating-point: no floating-point arithmetic is present.
- RNG: no RNG state is read or changed.
- Timestamp behavior: timestamps are still generated by `now_unix_ms` inside
  `EvidenceLedger::record`.
- Mode mutation: `RuntimeContext::new` stores the same initial mode, and
  `set_mode` still mutates `self.mode` before recording the switch entry.
- Public ledger behavior: `EvidenceLedger::new().is_empty()` remains true and
  custom event recording still appends in the same order.

Golden output:

```text
63ca5fec75ea6d605fff8496fac553e5c06ad9e2b1f93d4469e0ab9281c5282d  artifacts/optimization/golden_outputs/ft_runtime_policy_pass20.txt
```

## Rebenchmark

After via rch:

```text
worker: vmi1153651
command: CARGO_TARGET_DIR=target/turquoise-pine-ft-runtime-et85-after rch exec -- cargo bench -p ft-runtime --bench runtime_bench -- runtime_policy_evidence/new_and_switch_1024 --warm-up-time 1 --measurement-time 5 --sample-size 20
time: [182.08 us 193.87 us 209.96 us]
```

Confirmation after via rch:

```text
worker: vmi1153651
command: CARGO_TARGET_DIR=target/turquoise-pine-ft-runtime-et85-after-confirm rch exec -- cargo bench -p ft-runtime --bench runtime_bench -- runtime_policy_evidence/new_and_switch_1024 --warm-up-time 1 --measurement-time 5 --sample-size 20
time: [179.08 us 186.26 us 198.87 us]
```

The fresh bead baseline and the after confirmation were on different workers and
are effectively tied (`186.00 us` vs `186.26 us`). The same-worker old-code
comparison on `vmi1153651` improved from `242.46 us` to `186.26 us`, about
23.2 percent faster. Confidence is capped because the strongest before/after
pair reuses prior same-worker evidence rather than a fresh forced same-worker
baseline.

## Score

Impact 2 x Confidence 2 / Effort 1 = 4.0.

Decision: keep.

## Gates

- `sha256sum -c artifacts/optimization/golden_checksums.txt --ignore-missing`: pass.
- `git diff --check -- crates/ft-runtime/src/lib.rs artifacts/optimization/2026-06-03_ft_runtime_policy_ledger_capacity_frankentorch-et85.md .skill-loop-progress-TurquoisePine.md .beads/issues.jsonl`: pass.
- `CARGO_TARGET_DIR=target/turquoise-pine-ft-runtime-et85-test rch exec -- cargo test -p ft-runtime runtime_policy_evidence_golden_summary_matches_fixture -- --nocapture`: pass, 1 test.
- `CARGO_TARGET_DIR=target/turquoise-pine-ft-runtime-et85-fmt rch exec -- cargo fmt -p ft-runtime --check`: pass.
- `CARGO_TARGET_DIR=target/turquoise-pine-ft-runtime-et85-check rch exec -- cargo check -p ft-runtime --all-targets`: pass.
- `CARGO_TARGET_DIR=target/turquoise-pine-ft-runtime-et85-clippy rch exec -- cargo clippy -p ft-runtime --all-targets --no-deps -- -D warnings`: pass.
- `ubs crates/ft-runtime/src/lib.rs artifacts/optimization/2026-06-03_ft_runtime_policy_ledger_capacity_frankentorch-et85.md .skill-loop-progress-TurquoisePine.md .beads/issues.jsonl`: exit 0, no critical issues. UBS still reports existing `ft-runtime` test panic-surface inventory; built-in fmt, clippy, cargo check, test-build, audit, and deny checks passed.
